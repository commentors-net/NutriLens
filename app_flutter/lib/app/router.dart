/// Router configuration for FoodVision app.
/// To be extended as we add more screens.
///
/// Planned routes:
/// - /home → Home screen (today's summary, new meal button)
/// - /capture → Capture screen (multi-photo flow)
/// - /review → Review screen (grid of photos, retake/remove options)
/// - /results → Results screen (nutrition analysis + per-item macros)
/// - /nutrition-detail/:itemId → Nutrition detail for an item
///
/// TODO: Implement navigation routing (use go_router or similar once needed)
